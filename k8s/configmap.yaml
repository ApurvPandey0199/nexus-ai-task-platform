apiVersion: v1
kind: ConfigMap
metadata:
  name: nexus-config
  namespace: nexus-ai
data:
  PORT: "3001"
  NODE_ENV: "production"
  REDIS_HOST: "nexus-redis-service"
  REDIS_PORT: "6379"
  WORKER_CONCURRENCY: "5"
